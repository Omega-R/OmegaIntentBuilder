package com.omega_r.libs.omegaintentbuilder.types

enum class ImageTypes(val mimeType: String) {

  IMAGE(MimeTypes.IMAGE),
  IMAGE_PNG(MimeTypes.IMAGE_PNG),
  IMAGE_JPEG(MimeTypes.IMAGE_JPEG),
  IMAGE_GIF(MimeTypes.IMAGE_GIF),
  IMAGE_PJPEG(MimeTypes.IMAGE_PJPEG),
  IMAGE_SVG_XML(MimeTypes.IMAGE_SVG_XML),
  IMAGE_TIFF(MimeTypes.IMAGE_TIFF),
  IMAGE_WEBP(MimeTypes.IMAGE_WEBP)

}